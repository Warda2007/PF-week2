#include<iostream>
using namespace std;
main()
{
    string name1,name2;
    cout<<"Enter the first name: ";
    cin>>name1;
    cout<<"Enter the second name: ";
    cin>>name2;
    if(name1==name2){
        cout<<"Yes both names are same.";
    }else{
        cout<<"No both names are not same.";
    }

}